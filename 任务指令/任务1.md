````
# 网站内容与视觉进阶优化任务指令 (Spec)

作为顶尖的前端与UI交互专家，请根据以下四大任务模块，对我的个人作品集网站（涉及 `index.html`, `video-collection.html`, `script.js`, `style.css`）进行深度修改。

**注意：所有的修改必须同时同步到 `index.html` 和 `video-collection.html` 两个页面，保持高度一致。**

## 任务一：重塑「关于我」视觉层次（图像注入与伪元素毛玻璃）
目前Bento Grid略显单调，需要注入个人图片并利用CSS伪元素构建带模糊效果的深层背景。

**1. 替换个人头像**
在 `index.html` 的 `.bento-avatar` 中，删除现有的 SVG placeholder，插入真实的图片：
- 图片路径：`d:\作品集\证件照.jpg`（请转换为适合Web的本地相对路径或直接使用该路径测试）。
- CSS要求：使用 `<img src="..." alt="陈俊良" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover; z-index: 2; position: relative;">`。

**2. 为Bento卡片添加带模糊滤镜的背景底图**
在 `style.css` 中为以下三个模块添加专属的 Class，并利用 `::before` 伪元素插入背景图。
- **教育背景卡片** (`.card-education`)：插入 `d:\作品集\image copy.png`。
- **项目经历卡片** (`.card-projects`)：插入 `d:\作品集\image.png`。
- **智能传播项目卡片** (`.card-research`)：插入 `d:\作品集\image copy 2.png`。
- **CSS 渲染要求**（必须严格遵守以保证文字清晰）：
  ```css
  .card-education, .card-projects, .card-research {
      position: relative;
      z-index: 1;
  }
  .card-education > *, .card-projects > *, .card-research > * {
      position: relative;
      z-index: 2; /* 确保文字和图标在模糊层之上 */
  }
  .card-education::before, .card-projects::before, .card-research::before {
      content: '';
      position: absolute;
      inset: 0;
      background-size: cover;
      background-position: center;
      opacity: 0.15; /* 保持极简克制，不要太亮 */
      filter: blur(8px); /* 模糊背景增加纵深 */
      z-index: 0;
      pointer-events: none;
      transition: opacity 0.4s ease;
  }
  .card-education:hover::before, .card-projects:hover::before, .card-research:hover::before {
      opacity: 0.25; /* 鼠标悬浮时背景微微亮起 */
  }
  /* 分别指定图片 URL */
  .card-education::before { background-image: url('d:/作品集/image copy.png'); }
  .card-projects::before { background-image: url('d:/作品集/image.png'); }
  .card-research::before { background-image: url('d:/作品集/image copy 2.png'); }

````

## 任务二：「关于我」动态数据大屏效果 (Number Counter)

在 `index.html` 的 `.bento-stats` 模块中，将静态数据改为带千分位格式化的平滑跳动数字。

**1. 更新 HTML 数据：**

- 项目经历：将 `data-target="17"`，后缀改为 `+`。
- 触达用户：将 `data-target="500000"`，后缀改为 `+`。
- 示例：`<span class="stat-number" data-target="500000">0</span><span class="stat-suffix">+</span>`

**2. 优化 JS 动画逻辑：**

- 在 `script.js` 中找到 `statsObserver`，更新跳动逻辑，要求在渲染 `num.textContent` 时，使用 `.toLocaleString()` 加上千分位逗号（如 `500,000+`）。
- **清理冗余**：删除 `index.html` 底部 `<script>` 标签内重复的 `statObserver` 逻辑，统一使用 `script.js` 中的逻辑，避免冲突。

## 任务三：视频合集文案全面重构

请在 `index.html` 和 `video-collection.html` 中全局搜索并替换以下视频标题（包含 `<h3>` 标签和相关的描述文本）：

**AI 广告片板块：**

- 广告片 1 替换为 -> `爆炸盐，炸掉所有烦恼`
- 广告片 2 替换为 -> `沐萌美，油污跑光亮晶晶`
- 广告片 3 替换为 -> `一滴浓，一身净`
- 广告片 4 替换为 -> `油污净，秒除污`
- 广告片 5 替换为 -> `AD钙，用心包裹儿时科幻梦`
- 广告片 6 替换为 -> `陪你酸酸甜甜，陪你可可爱爱`

**长 AI 游戏视频板块：**

- 长游戏AI视频 1 替换为 -> `三角洲委托——多元素引流`
- 长游戏AI视频 2 替换为 -> `三角洲委托——冲突化引流`
- 长游戏AI视频 3 替换为 -> `三角洲委托——剧情化引流`
- 长游戏AI视频 4 替换为 -> `三角洲委托——洛克王国与三角洲的剧情化、冲突化叙事`
- 长游戏AI视频 5 替换为 -> `三角洲委托——真人化仿真叙事`
- 长游戏AI视频 6 替换为 -> `三角洲委托——生活化叙事引流`
- 长游戏AI视频 7 替换为 -> `短剧化AI视频`

## 任务四：技术栈信息精准勘误

- 找到现在的“三角洲委托——真人化仿真叙事”（原 长游戏AI视频 5），将其下方的 **AI工具** 描述修改为：`Sora、Kling、Runway Gen-3`。
- 找到现在的“短剧化AI视频”（原 长游戏AI视频 7），将其下方的 **AI工具** 描述修改为：`Sora、Kling、Pika Labs`。

请按以上任务逐一修改，确保修改后的页面没有图片层级遮挡文字的问题，并且大数字滚动动画平滑且带千分位逗号。完成后向我报告。
