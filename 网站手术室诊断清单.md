# 🏥 网站手术室诊断清单

> **诊断对象**: www.chenjunliang.top  
> **诊断时间**: 2026-05-22  
> **诊断工具**: Playwright 自动化浏览 + 源代码审查  
> **诊断页面**: 首页、分类中心、视频播放器（4个分类）、视频合集页、移动端适配

---

## 📊 诊断概览

| 指标 | 数值 | 状态 |
|------|------|:----:|
| 审计页面数 | 9 | ✅ |
| 控制台警告 | 12 | ⚠️ |
| 页面 JS 错误 | 4 | 🔴 |
| 桌面端首页加载时间 | 16,340ms | 🔴 |
| 移动端首页加载时间 | 6,197ms | ⚠️ |
| 视频合集页加载时间 | 766ms | ✅ |
| 移动端布局溢出元素 | 20个 | 🔴 |
| 字体解码失败 | 1个 | 🔴 |

---

## 🔴 严重问题（Critical）

### C-01: 字体文件损坏 — LiuJianMaoCao-Regular.ttf

- **现象**: 控制台报错 `Failed to decode downloaded font` + `OTS parsing error: invalid sfntVersion: 168430090`
- **影响范围**: 所有页面（首页、视频合集页、移动端均出现，共12条警告）
- **根因**: `fonts/LiuJianMaoCao-Regular.ttf` 文件的 sfntVersion 值为 `0x0A000A0A`，不是合法的 TrueType/OpenType 签名，浏览器无法解码
- **影响**: 使用 `--font-brush` CSS 变量的元素（落地页标题、hub分类文字等）回退到 serif 字体，视觉设计意图无法实现
- **涉及文件**:
  - `fonts/LiuJianMaoCao-Regular.ttf`（损坏的字体文件）
  - `style.css:20-23`（@font-face 声明）
  - `style.css:55`（`--font-brush: 'LiuJianMaoCao', 'ZhiMangXing', serif`）
- **优化建议**:
  1. 重新下载 LiuJianMaoCao 字体的正确版本
  2. 或使用 Google Fonts CDN 替代本地加载
  3. 或将 TTF 转换为 WOFF2 格式（更小、加载更快）
  4. 添加 `font-display: swap` 已有，但应确保 fallback 字体更接近目标效果

### C-02: JavaScript 空引用错误 — addEventListener on null

- **现象**: 控制台报错 `Cannot read properties of null (reading 'addEventListener')`
- **影响范围**: 所有页面（首页、视频合集页、移动端均出现，共4条错误）
- **根因**: `script.js` 被 `index.html` 和 `video-collection.html` 共同引用，但 `script.js` 中的代码引用了仅存在于 `index.html` 的 DOM 元素：
  - `script.js:89` — `document.getElementById('enter-profile')` 在 video-collection.html 中不存在
  - `script.js:130` — `document.getElementById('profile-back')` 在 video-collection.html 中不存在
  - `script.js:165` — `document.getElementById('hub-back')` 在 video-collection.html 中不存在
  - 以及其他 index.html 专属元素
- **影响**: JS 执行中断，可能导致 video-collection.html 上的后续功能（如视频播放交互）失效
- **涉及文件**: `script.js:89, 130, 165, 257, 370, 433, 460, 465, 473, 479, 495`
- **优化建议**:
  1. 在所有 `getElementById` 后添加 null 检查：`var el = document.getElementById('xxx'); if (el) el.addEventListener(...)`
  2. 或将 script.js 拆分为 `index.js`（首页专用）和 `collection.js`（合集页专用）
  3. 或使用事件委托模式，减少对特定 DOM 元素的直接依赖

### C-03: 移动端视频元素溢出 — video-collection.html

- **现象**: 19个 `<video>` 元素在 375px 视口下宽度为 578-640px，溢出 203-265px
- **影响范围**: 视频合集页移动端（所有手机用户）
- **根因**: `video-collection.html` 中的 `<video>` 元素没有设置 `max-width: 100%`，且 `.video-wrapper` 容器缺少 `overflow: hidden` 和响应式宽度约束
- **影响**: 移动端用户需要横向滚动才能看到完整视频，严重影响用户体验
- **涉及文件**: `video-collection.html`（所有 video 元素）、`style.css`（缺少 video-wrapper 响应式样式）
- **优化建议**:
  1. 为 `.video-wrapper` 添加 `overflow: hidden; max-width: 100%;`
  2. 为 `.video-wrapper video` 添加 `max-width: 100%; height: auto;`
  3. 在 `@media (max-width: 768px)` 中添加 `.video-gallery { grid-template-columns: 1fr !important; }`

---

## 🟡 中等问题（Medium）

### M-01: 首页加载时间过长 — 16.3秒

- **现象**: 桌面端首页 `domComplete` 耗时 15,540ms，总加载时间 16,340ms
- **影响范围**: 首页所有用户
- **根因分析**:
  - TTFB 仅 94ms（服务器响应快）
  - DOM 解析仅 58ms（HTML 本身不大）
  - domComplete 耗时异常，可能原因：
    1. 字体文件解码失败导致浏览器等待超时
    2. 7个 TTF 字体文件总体积较大（中文字体通常 5-15MB/个）
    3. 字体未使用 WOFF2 压缩格式
    4. 缺少资源加载优先级控制
- **影响**: 用户等待时间过长，首屏白屏时间久，跳出率高
- **涉及文件**: `style.css:1-42`（7个 @font-face 声明）、`fonts/` 目录
- **优化建议**:
  1. 将所有 TTF 字体转换为 WOFF2 格式（体积减少 30-50%）
  2. 使用 `font-display: optional` 替代 `swap`，避免字体加载阻塞渲染
  3. 对非首屏字体使用 `preload` + `onload` 延迟加载
  4. 考虑使用 Google Fonts CDN 或字体子集化（仅加载使用到的字符）
  5. 修复 C-01 字体损坏问题，消除无效等待

### M-02: 首页 .landing-grain 移动端溢出

- **现象**: `.landing-grain` 元素宽度 750px，在 375px 视口下溢出 375px
- **影响范围**: 移动端首页
- **根因**: `style.css:163-173` 中 `.landing-grain` 使用 `width: 200%; inset: -50%`，在移动端未做特殊处理
- **影响**: 可能导致移动端出现横向滚动条
- **涉及文件**: `style.css:163-173`
- **优化建议**:
  1. 在 `@media (max-width: 768px)` 中为 `.landing-grain` 添加 `overflow: hidden` 到父容器
  2. 或在移动端减小 grain 尺寸：`width: 100%; inset: 0;`

### M-03: 视频合集页样式定义缺失

- **现象**: `video-collection.html` 使用了 `.video-card`、`.video-wrapper`、`.video-overlay`、`.play-btn`、`.video-loading`、`.video-error`、`.video-info` 等类名，但 `style.css` 中没有定义这些样式
- **影响范围**: 视频合集页的视觉呈现和交互
- **根因**: 这些样式可能定义在 `video-collection.html` 的内联 `<style>` 中（行11-255），但未在 `style.css` 中统一管理
- **影响**: 样式分散维护困难，且可能与全局样式冲突
- **涉及文件**: `video-collection.html:11-255`、`style.css`
- **优化建议**:
  1. 将 video-collection.html 的内联样式迁移到 `style.css` 中统一管理
  2. 或创建独立的 `video-collection.css` 文件

---

## 🟢 轻微问题（Low）

### L-01: 字体格式未优化 — 全部使用 TTF

- **现象**: 7个 @font-face 全部使用 TTF 格式，未提供 WOFF2 回退
- **影响范围**: 所有页面加载速度
- **根因**: `style.css:1-42` 仅声明了 `format('truetype')`
- **影响**: 字体文件体积大，加载慢，浪费带宽
- **涉及文件**: `style.css:1-42`、`fonts/` 目录
- **优化建议**:
  1. 为每个字体生成 WOFF2 版本
  2. 使用 `@font-face` 的多 src 声明，优先加载 WOFF2：
     ```css
     src: url('fonts/xxx.woff2') format('woff2'),
          url('fonts/xxx.ttf') format('truetype');
     ```

### L-02: 缺少 favicon 和 PWA 支持

- **现象**: 网站没有 favicon.ico 和 manifest.json
- **影响范围**: 浏览器标签页图标、移动端添加到主屏幕
- **优化建议**:
  1. 添加 `<link rel="icon" href="favicon.ico">`
  2. 添加 `<link rel="manifest" href="manifest.json">`
  3. 创建 PWA manifest 文件

### L-03: 缺少 Open Graph 和社交媒体元标签

- **现象**: HTML 中缺少 og:title、og:description、og:image 等社交分享标签
- **影响范围**: 微信、微博等社交平台分享时的展示效果
- **涉及文件**: `index.html:1-11`、`video-collection.html:1-11`
- **优化建议**:
  1. 添加 Open Graph 标签：
     ```html
     <meta property="og:title" content="陈俊良 | 个人作品集">
     <meta property="og:description" content="...">
     <meta property="og:image" content="...">
     <meta property="og:url" content="https://www.chenjunliang.top">
     ```

### L-04: 视频资源未使用懒加载

- **现象**: `video-collection.html` 中所有 19 个 `<video>` 元素都设置了 `preload="none"`，但缺少 Intersection Observer 懒加载逻辑
- **影响范围**: 视频合集页的带宽消耗和加载速度
- **优化建议**:
  1. 使用 Intersection Observer 在视频进入视口时才加载 poster 和视频源
  2. 考虑使用 `loading="lazy"` 属性（部分浏览器支持）

### L-05: 缺少 404 页面

- **现象**: 网站没有自定义 404 页面
- **影响范围**: 用户访问不存在的 URL 时的体验
- **优化建议**:
  1. 创建 `404.html` 页面，与网站设计风格一致
  2. 提供返回首页和视频合集页的链接

### L-06: CSS 变量未完全统一

- **现象**: `video-collection.html` 内联样式中使用了 `var(--glass)`、`var(--glass-border)`、`var(--text)` 等变量，但部分样式仍使用硬编码值
- **影响范围**: 主题切换一致性
- **优化建议**:
  1. 审查所有内联样式，将硬编码颜色值替换为 CSS 变量
  2. 确保深色/浅色模式下所有元素颜色正确切换

---

## 📋 优化优先级排序

| 优先级 | 编号 | 问题 | 预计影响 |
|:------:|:----:|------|----------|
| P0 | C-02 | JS 空引用错误 | 修复后 video-collection.html 功能恢复正常 |
| P0 | C-03 | 移动端视频溢出 | 修复后移动端用户体验大幅提升 |
| P1 | C-01 | 字体文件损坏 | 修复后视觉设计意图正确实现 |
| P1 | M-01 | 首页加载过慢 | 优化后加载时间可降至 3-5 秒 |
| P2 | M-02 | grain 移动端溢出 | 修复后消除横向滚动条 |
| P2 | M-03 | 样式定义分散 | 整理后维护性提升 |
| P3 | L-01 | 字体格式优化 | 优化后带宽节省 30-50% |
| P3 | L-02 | 缺少 favicon/PWA | 完善网站基础功能 |
| P3 | L-03 | 缺少社交元标签 | 提升社交分享效果 |
| P3 | L-04 | 视频懒加载 | 优化带宽和加载速度 |
| P3 | L-05 | 缺少 404 页面 | 完善用户体验 |
| P3 | L-06 | CSS 变量未统一 | 提升主题切换一致性 |

---

## 🔬 诊断数据附录

### 页面加载性能

| 页面 | TTFB | DOM解析 | domComplete | 总加载时间 |
|------|------|---------|-------------|-----------|
| 首页（桌面） | 94ms | 58ms | 15,540ms | 16,340ms |
| 首页（移动） | 70ms | 14ms | 5,896ms | 6,197ms |
| 视频合集（桌面） | 180ms | 489ms | 568ms | 766ms |
| 视频合集（移动） | 64ms | 430ms | 426ms | 508ms |

### 控制台错误统计

| 错误类型 | 出现次数 | 涉及页面 |
|----------|---------|---------|
| 字体解码失败 | 6次 | 首页、合集页、移动端 |
| OTS 解析错误 | 6次 | 首页、合集页、移动端 |
| JS 空引用 | 4次 | 首页、合集页、移动端 |

### 移动端布局溢出

| 页面 | 溢出元素数 | 最大溢出量 |
|------|-----------|-----------|
| 首页 | 1 | 375px (.landing-grain) |
| 视频合集页 | 19 | 265px (video 元素) |
